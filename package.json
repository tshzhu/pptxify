{
  "name": "pptxify",
  "version": "1.0.0",
  "description": "Convert PDF pages to high-quality image-based PPTX files in the browser or with the local CLI.",
  "homepage": "https://tshzhu.github.io/pptxify/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tshzhu/pptxify.git"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "bin": {
    "pptxify": "dist-cli/cli.js"
  },
  "files": [
    "dist-cli/",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:cli": "tsc -p tsconfig.cli.json",
    "build:all": "npm run build && npm run build:cli",
    "pack:check": "node scripts/check-package.mjs",
    "prepublishOnly": "npm run check && npm run build:cli && npm run pack:check",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@napi-rs/canvas": "0.1.100",
    "@xmldom/xmldom": "0.8.15",
    "jszip": "3.10.1",
    "pdfjs-dist": "5.4.624",
    "pptxgenjs": "4.0.1"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "vite": "8.2.2"
  }
}
